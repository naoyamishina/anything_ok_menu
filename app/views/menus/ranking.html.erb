<% assign_meta_tags title: (t '.title') %>
<div class="container pt-3 mb-5">
  <%= render 'content_top' %>

  <%
=begin%>
 <div class="row">
      <!-- 検索フォーム -->
      <%= render 'search_form', url: menus_path %>
  </div> 
<%
=end%>
  <!-- 掲示板一覧 -->
  <div class="row">
    <div class="col-12">
      <div class="row">
        <% if @menus.present? %>
          <%= render @menus %>
        <% else %>
          <p><%= t('.no_result') %></p>
        <% end %>
      </div>
    </div>
  </div>
</div>