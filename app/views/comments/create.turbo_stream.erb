<%= turbo_stream.prepend "comments" do %>
  <%= render @comment %>
<% end %>

<%= turbo_stream.update "comment_form" do %>
  <%= render 'comments/form', { menu: @menu, comment: Comment.new } %>
<% end %>
